version: '3'
services:
  trello:
    # image: thinhbuihong/trello
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:4000"
    volumes:
      - /app/node_modules
      - .:/app
  # tests:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   volumes:
  #     - /app/node_modules
  #     - .:/app
  #   command: ["yarn","test"]
